# Ative com SPRING_PROFILES_ACTIVE=prod.
# Senha padrão do PostgreSQL de demonstração; pode ser substituída por SPRING_DATASOURCE_PASSWORD.
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:hjgiuiBOUVfdslf132@BRU32$NSDGF}
spring.thymeleaf.cache=true
spring.jpa.show-sql=false

# /tmp é gravável em praticamente todas as plataformas. O conteúdo é temporário.
aplicacao.upload.cursos=${UPLOAD_DIR:/tmp/ki/uploads/cursos}

server.error.include-message=never
server.error.include-binding-errors=never
