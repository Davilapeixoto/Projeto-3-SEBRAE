# Integração realizada

- Projeto do front usado como base.
- `frontend/src/main.jsx`, componentes React e estilos do front preservados.
- Backend Spring Boot completo incorporado.
- Página inicial Thymeleaf do front preservada e conectada às rotas reais.
- Login e cadastro do front preservados, com validações compatíveis com o backend.
- Painel administrativo e páginas de cursos vieram do backend funcional.
- Arquivos gerados antigos em `target/` foram removidos para evitar classes desatualizadas.

## Acesso administrativo padrão

- E-mail: `admin@gmail.com`
- Senha: `admin123`

As credenciais podem ser alteradas pelas variáveis `ADMIN_EMAIL`, `ADMIN_PASSWORD` e `ADMIN_NAME`.
